$(function(){
/*
    1. create a div d-block
    2. append a div d-flex
    3. set height, width, padding, border-radius of d-flex with an input for search
    4. create a div d-block
    5. capture the target to init the select3 (the actual)
    6. add d-none to target
    7. get the options of select(the actual) & iterate thru
    8. get text & value of option and put it on a div created in iteration of options
    9. add onclick on each opt div created
    10. it will create a new div (put the value of selected opt in the actual target select)
    11. create a div now with innerText of the opt selected (class d-flex)
    12. add a close button to d-flex of above step(justify-content space-between) & add borderRadius with bgColor
    13. add onclick on close button (remove value of target select & removeChild from the div of step-2)
    14. put all the opts created in a newly created div &
    15. append it to the step-1 div
    16.
    17.
*/

})
